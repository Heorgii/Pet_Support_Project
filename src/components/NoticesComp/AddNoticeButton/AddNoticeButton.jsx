import { AddNoticeModal } from "./AddNoticeModal/AddNoticeModal";

export const AddNoticeButton = () => {
    return (
        <div>
            <h1>Add Notice Button</h1>
            <AddNoticeModal />
        </div>
    );
}