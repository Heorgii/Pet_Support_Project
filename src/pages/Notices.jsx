import { Helmet } from "react-helmet";

export const Notices = () => {
    return (
        <div>
            <Helmet>
                <title>Find pet</title>
            </Helmet>
            <h1>Notices / Find pet page</h1>
        </div>
    );
}