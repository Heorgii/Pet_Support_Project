import { Helmet } from "react-helmet";

export const User = () => {
    return (
        <div>
            <Helmet>
                <title>Profile</title>
            </Helmet>
        </div>
    );
}